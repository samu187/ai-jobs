{
  "name": "ai-jobs-frontend",
  "private": true,
  "scripts": {
    "build": "esbuild frontend/App.jsx --bundle --minify --outfile=src/jobs/static/app.js",
    "test": "node --test frontend/*.test.mjs",
    "test:browser": "playwright test"
  },
  "dependencies": {
    "react": "19.3.0",
    "react-dom": "19.3.0"
  },
  "devDependencies": {
    "esbuild": "0.25.12",
    "@playwright/test": "1.58.2"
  },
  "type": "module"
}
